const URL = "http://localhost:3000";
const GET_ALBERGUES = `${URL}/albergues`;
const GET_COMEDORES = `${URL}/comedores`;
const REGISTER_USER = `${URL}/users/register`;
const LOGIN_USER = `${URL}/users/authenticate`;
const LOGOUT_USER = `${URL}/users/logout`;

export {
    URL,
    GET_ALBERGUES,
    GET_COMEDORES,
    REGISTER_USER,
    LOGIN_USER,
    LOGOUT_USER,
  };